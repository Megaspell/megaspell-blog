import styled from 'styled-components';

const Wrapper = styled.header`
  margin: 0 auto;
  max-width: 650px;
  padding: 0 50px 50px;
  text-align: center;
  filter: drop-shadow(0 0 40px rgba(0, 255, 153, 0.2));
`;

export default Wrapper;